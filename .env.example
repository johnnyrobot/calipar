# =============================================================================
# CALIPAR Platform Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values
# See docs/FIREBASE_SETUP.md for detailed Firebase configuration instructions
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# For Docker: use docker-compose defaults
DATABASE_URL=postgresql://calipar:calipar_dev_password@db:5432/calipar
# For local development without Docker:
# DATABASE_URL=sqlite:///./calipar_dev.db

# Demo Database (separate from production database for demo users)
# Leave empty to disable demo mode
# For SQLite demo database: sqlite:///./calipar_demo.db
# For PostgreSQL demo database (same server, different DB): postgresql://calipar:password@db:5432/calipar_demo
DEMO_DATABASE_URL=

# -----------------------------------------------------------------------------
# Demo Mode Configuration
# -----------------------------------------------------------------------------
# Enable demo mode (allows users with "demo" in email to access demo environment)
DEMO_MODE_ENABLED=false
# Hour of day to reset demo database (UTC, 0-23). Default: 7 = midnight PST
DEMO_RESET_HOUR_UTC=7
# Prefix that identifies demo users (users with this in email/uid are demo users)
DEMO_USER_PREFIX=demo

# -----------------------------------------------------------------------------
# Google AI / Gemini Configuration
# -----------------------------------------------------------------------------
# Get your API key from: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=your_google_api_key_here
# File Search store ID (created during document ingestion)
GEMINI_FILE_SEARCH_STORE_NAME=your_file_search_store_id

# -----------------------------------------------------------------------------
# Firebase Authentication - Backend
# -----------------------------------------------------------------------------
# Your Firebase project ID (from Firebase Console > Project Settings)
FIREBASE_PROJECT_ID=your_firebase_project_id
# Path to your service account key (download from Firebase Console)
# Default: ./backend/serviceAccountKey.json
# FIREBASE_SERVICE_ACCOUNT_PATH=./backend/serviceAccountKey.json

# -----------------------------------------------------------------------------
# Firebase Authentication - Frontend
# -----------------------------------------------------------------------------
# IMPORTANT: All frontend Firebase vars need NEXT_PUBLIC_ prefix
# Get these values from Firebase Console > Project Settings > Your apps > Web app
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSy...your_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_firebase_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789012
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789012:web:abc123def456

# Optional: Use Firebase Auth Emulator for local development
# Start emulator with: firebase emulators:start --only auth
# NEXT_PUBLIC_FIREBASE_AUTH_EMULATOR_URL=http://localhost:9099

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
# Enable debug mode (true for development, false for production)
DEBUG=true
# Secret key for session management (MUST change in production!)
SECRET_KEY=calipar-dev-secret-key-change-in-production
# Backend API URL (used by frontend)
NEXT_PUBLIC_API_URL=http://localhost:8000
